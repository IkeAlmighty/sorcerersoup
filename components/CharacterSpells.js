import { useState } from "react";

export default function CharacterInventory({ _id, initialValue }) {
  // TODO: impl socket io integration
  async function updateCharacterSpells() {
    // update database:
    // update socketio:
  }
  return (
    <div className="my-6">
      TODO: this will integrate with socket io to allow GM to OR this client to
      edit
    </div>
  );
}
